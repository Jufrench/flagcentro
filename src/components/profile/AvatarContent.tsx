// import { Avatar, Button, DEFAULT_THEME, Paper, Stack, Title } from "@mantine/core";
// import { User } from "../../types";
import { Dropzone } from "@mantine/dropzone";

export default function AvatarContent() {
  return (
    <Dropzone onDrop={() => {}}></Dropzone>
  );
}